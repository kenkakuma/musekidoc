# AI Agent 内容研究与填充指南

**目标**：AI agent 自主研究日本陶艺知识，填充知识库内容

**用户角色**：内容审核和细节调整

---

## 🎯 内容填充目标

| 阶段 | 内容目标 | AI 角色 | 用户角色 |
|------|----------|---------|----------|
| 阶段 1 | 0 → 50 条 | 主动研究 + 填充 | 验证准确性、调整细节 |
| 阶段 2 | 50 → 500 条 | 批量研究 + 填充 | 抽查质量、纠正错误 |
| 阶段 3 | 500+ 条 | 深度研究 + 补充 | 专业审核、优化表达 |

---

## 📚 内容研究流程（AI Agent 必读）

### 第一步：选择研究主题

**主题来源**：
1. **六古窑体系**（优先）
   - 备前烧、常滑烧、瀬戸烧、信楽烧、丹波烧、越前烧
   - 每个窑口包含：总览、历史、技法、代表作家

2. **人间国宝名单**（权威来源）
   - 从 https://www.ceramic.or.jp/museum/yakimono/contents/kokuho.html
   - 提取所有陶磁领域人间国宝
   - 为每位作家创建条目

3. **当代热门作家**（市场导向）
   - Instagram 粉丝数 > 1 万的日本陶艺家
   - 示例：八田亨、尾形笃、小野哲平、安藤雅信

4. **重要技法分类**
   - 志野、织部、黄瀬戸、粉引、三島、刷毛目等
   - 乐烧、唐津烧、萩烧等地方特色技法

5. **重要产地**
   - 有田、九谷、萩、美濃、京都等
   - 每个产地的历史、特色、代表作家

**选择原则**：
- ✅ 优先填充基础框架（六古窑、人间国宝）
- ✅ 平衡传统与当代（70% 传统 + 30% 当代）
- ✅ 确保信息可验证（有权威来源）

---

### 第二步：信息收集（使用 WebSearch 和 WebFetch）

#### 必须收集的信息

```yaml
基础信息:
  - 中文名称（翻译准确）
  - 日文名称（原文）
  - 英文名称（国际通用）
  - 分类（陶器/磁器/作家/产地/技法）
  - 产地（具体到市町村）

核心内容:
  - 描述（300-500 字，中文）
    - 历史背景
    - 技术特点
    - 艺术风格
    - 文化意义
  - 定位说明（1-2 句话概括）

识别特征:
  - 3-5 个显著特征（箇条書き）
  - 例如："无釉高温烧成"、"窯变分类：胡麻/緋襷"

关键词:
  - 5-10 个（中日文混合）
  - 用于搜索和分类

作家信息（如适用）:
  - 出生年份
  - 师承关系
  - 代表作品
  - 荣誉称号（人间国宝、日本艺术院会员等）
  - Instagram 账号和粉丝数
  - 展览次数/市场价格区间（如有）

代表器型:
  - 茶碗、花器、皿、壺等
  - 每种器型的特点

来源:
  - 至少 2-3 个权威来源
  - 优先：官方网站、博物馆、学术文章
  - 格式：{"title": "来源标题", "url": "完整 URL"}
```

#### 推荐信息源

**权威来源**（必须使用）：
1. **日本陶磁协会**
   - https://www.ceramic.or.jp/
   - 人间国宝名单、陶磁历史

2. **东京文化财研究所**
   - https://www.tobunken.go.jp/materials/bukko/
   - 作家传记数据库（最权威）

3. **各地陶磁美术馆**
   - 濑户藏博物馆、备前陶艺美术馆等

4. **六古窑官方网站**
   - https://en.sixancientkilns.jp/

**补充来源**：
5. **日本观光局 (JNTO)**
   - https://www.japan.travel/
   - 产地介绍

6. **地方政府网站**
   - 例如：濑户市、备前市文化财清单

7. **当代作家**：
   - 作家官网
   - Instagram 官方账号
   - 画廊代理信息

**禁止使用**：
- ❌ 不可靠的个人博客
- ❌ 商业广告内容（除非是作家官方商店）
- ❌ 未标注来源的维基百科内容

---

### 第三步：信息验证

**必须验证的内容**：

1. **名称准确性**
   - 日文名称必须是正式用语（不是简化或俗称）
   - 中文翻译符合学术规范
   - 英文名称是国际通用名

2. **历史事实**
   - 人间国宝认定年份（交叉对比东文研数据库）
   - 出生/逝世年份
   - 窑口成立时间

3. **技术术语**
   - 陶艺术语使用正确（如"焼締"vs"焼き締め"）
   - 釉药名称、技法名称准确

4. **来源链接**
   - 所有 URL 必须可访问（使用 WebFetch 验证）
   - 链接内容与描述匹配

**验证方法**：
```
1. 使用 WebSearch 搜索关键信息
2. 至少找到 2 个独立来源确认
3. 如有冲突，使用官方/学术来源为准
4. 无法验证的信息标注"待确认"，不要编造
```

---

### 第四步：数据填充

#### 方式 A：通过管理后台表单（推荐）

**适用场景**：单条添加，需要上传图片

**步骤**：
1. 访问 `http://localhost:3000/admin/entries/new`
2. 按照表单逐字段填写：
   ```
   中文名称：备前烧（总览）
   日文名称：備前焼
   英文名称：Bizen ware
   Slug：bizen-yaki（自动生成，可修改）

   分类：陶器 / 无釉 / 薪窑（焼締）
   产地：冈山县・备前市（伊部/Imbe）
   定位：以窯变"景色"为核心；名家体系清晰（人间国宝）

   描述（Markdown）：
   备前烧是日本六古窑之一，产于冈山县备前市...

   识别特征（每行一个）：
   - 无釉高温烧成（焼締）
   - 窯变分类：胡麻/緋襷/桟切/窯変
   - 名家：金重陶陽、藤原雄、伊勢崎淳等

   关键词（标签形式）：
   備前焼, 無釉, 焼締, 窯変, 人間国宝

   知名作家：
   - 金重陶陽
   - 藤原雄
   - 伊勢崎淳

   代表器型：
   - 茶碗
   - 徳利
   - ぐい呑

   来源：
   [添加来源] 标题：六古窯：Bizen
              URL：https://en.sixancientkilns.jp/bizen/
   ```

3. 上传图片（如有）：
   - 使用 WebFetch 下载图片（确保版权允许）
   - 拖拽上传到表单
   - 添加 alt 文本和 caption

4. 预览检查
5. 保存（发布或保存为草稿）

---

#### 方式 B：批量导入（JSON）

**适用场景**：批量添加 10+ 条，暂无图片

**步骤**：

1. 创建 JSON 文件（`data/pottery-entries-batch-1.json`）：

```json
[
  {
    "nameZh": "备前烧（总览）",
    "nameJa": "備前焼",
    "nameEn": "Bizen ware",
    "slug": "bizen-yaki",
    "category": "陶器 / 无釉 / 薪窑（焼締）",
    "region": "冈山县・备前市",
    "positioning": "以窯变"景色"为核心；名家体系清晰（人间国宝）",
    "description": "备前烧是日本六古窑之一...",
    "signatureFeatures": [
      "无釉高温烧成（焼締）",
      "窯变分类：胡麻/緋襷/桟切/窯変",
      "名家：金重陶陽、藤原雄、伊勢崎淳等"
    ],
    "keywords": ["備前焼", "無釉", "焼締", "窯変", "人間国宝"],
    "notableArtists": ["金重陶陽", "藤原雄", "伊勢崎淳"],
    "representativeForms": ["茶碗", "徳利", "ぐい呑", "花器", "壺"],
    "sources": [
      {
        "title": "六古窯：Bizen",
        "url": "https://en.sixancientkilns.jp/bizen/"
      }
    ],
    "published": false
  },
  {
    "nameZh": "常滑烧（总览）",
    "nameJa": "常滑焼",
    // ... 下一条
  }
]
```

2. 访问 `http://localhost:3000/admin/bulk-import`
3. 上传 JSON 文件
4. 预览数据（检查错误）
5. 确认导入

---

#### 方式 C：API 调用（自动化）

**适用场景**：编写脚本批量处理

```javascript
// scripts/auto-fill-content.js
const entries = [
  // ... 数据数组
]

for (const entry of entries) {
  const response = await fetch('http://localhost:3000/api/entries', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.ADMIN_TOKEN}`
    },
    body: JSON.stringify(entry)
  })

  if (response.ok) {
    console.log(`✅ 成功添加：${entry.nameZh}`)
  } else {
    console.error(`❌ 失败：${entry.nameZh}`, await response.json())
  }

  // 避免过快请求
  await new Promise(resolve => setTimeout(resolve, 1000))
}
```

---

### 第五步：质量检查（用户介入点）

**AI 完成后，提交给用户审核**：

1. **在 DAILY-LOG.md 中记录**：
   ```markdown
   ## ✅ 今日完成内容

   - 添加 10 条备前烧相关条目
   - 条目 ID: entry-001 ~ entry-010
   - 状态：草稿（待用户审核）
   - 预览链接：http://localhost:3000/pottery/bizen-yaki

   **请用户检查**：
   - [ ] 中文表达是否自然
   - [ ] 日文名称是否准确
   - [ ] 历史事实是否正确
   - [ ] 来源链接是否有效
   ```

2. **用户审核流程**：
   - 登录后台：`http://localhost:3000/admin/entries`
   - 筛选"草稿"状态
   - 逐条审核，修改细节
   - 确认无误后，修改状态为"已发布"

---

## 📋 内容填充计划（分批次）

### 批次 1：六古窑框架（10 条）

**目标**：建立基础分类体系

```
1. 六古窯（知识库标签） - 总览条目
2. 备前烧（总览）
3. 常滑烧（总览）
4. 瀬戸烧（总览）
5. 信楽烧（总览）
6. 丹波烧（总览）
7. 越前烧（总览）
8. 人间国宝（陶磁）名单入口
9. 陶器与磁器的区别（基础知识）
10. 日本陶艺历史简述
```

**预估时间**：AI 2-3 小时研究 + 1 小时填充 = 半天

---

### 批次 2：人间国宝作家（20 条）

**目标**：权威作家数据库

**步骤**：
1. 从日本陶磁协会获取完整名单
2. 每位作家创建一条
3. 包含：生平、技法、代表作、荣誉

**来源**：
- https://www.ceramic.or.jp/museum/yakimono/contents/kokuho.html
- https://www.tobunken.go.jp/materials/bukko/

**预估时间**：AI 4-5 小时研究 + 2 小时填充 = 1 天

---

### 批次 3：当代热门作家（20 条）

**目标**：吸引年轻用户，关联商品销售

**研究重点**：
- Instagram 粉丝数
- 展览频率
- 市场价格区间
- 作品风格特点

**示例作家**：
- 八田亨（Hatta Toru）
- 尾形笃（Ogata Atsushi）
- 小野哲平（Ono Teppei）
- 安藤雅信（Ando Masanobu）
- 小林千恵（Kobayashi Chie）

**预估时间**：AI 3-4 小时研究 + 2 小时填充 = 1 天

---

### 批次 4：重要技法（15 条）

**目标**：技术知识体系

```
1. 志野（Shino）
2. 织部（Oribe）
3. 黄瀬戸（Ki-Seto）
4. 粉引（Kohiki）
5. 三島（Mishima）
6. 刷毛目（Hakeme）
7. 乐烧（Raku）
8. 唐津烧（Karatsu）
9. 萩烧（Hagi）
10. 伊万里/有田（Imari/Arita）
11. 九谷烧（Kutani）
12. 益子烧（Mashiko）
13. 小鹿田烧（Onta）
14. 波佐见烧（Hasami）
15. 美浓烧（Mino）
```

**预估时间**：AI 5-6 小时研究 + 3 小时填充 = 1.5 天

---

## 🤖 AI Agent 自动化脚本（参考）

### 脚本 1：自动研究并生成 JSON

```javascript
// scripts/ai-research-and-generate.js
import { WebSearch, WebFetch } from './utils/web-tools.js'

async function researchTopic(topic) {
  console.log(`🔍 研究主题：${topic}`)

  // 1. 搜索基础信息
  const searchResults = await WebSearch(`${topic} 日本陶艺 特点`)

  // 2. 提取权威来源
  const authoritativeSources = searchResults.filter(r =>
    r.url.includes('ceramic.or.jp') ||
    r.url.includes('tobunken.go.jp') ||
    r.url.includes('.go.jp')
  )

  // 3. 抓取详细内容
  const details = []
  for (const source of authoritativeSources.slice(0, 3)) {
    const content = await WebFetch(source.url)
    details.push({ source: source.url, content })
  }

  // 4. 使用 AI 提取结构化数据
  const structuredData = await extractStructuredData(details, topic)

  // 5. 验证数据完整性
  validateData(structuredData)

  // 6. 返回 JSON
  return structuredData
}

async function extractStructuredData(details, topic) {
  // 调用 AI 模型，提取关键信息
  // 返回符合 schema 的 JSON 对象
  return {
    nameZh: "...",
    nameJa: "...",
    // ... 其他字段
  }
}

function validateData(data) {
  // 验证必填字段
  const required = ['nameZh', 'nameJa', 'category', 'region', 'description']
  for (const field of required) {
    if (!data[field]) {
      throw new Error(`缺少必填字段：${field}`)
    }
  }

  // 验证 URL 有效性
  for (const source of data.sources) {
    if (!source.url.startsWith('http')) {
      throw new Error(`无效的 URL：${source.url}`)
    }
  }
}

// 批量处理
const topics = ['備前焼', '常滑焼', '瀬戸焼', '信楽焼']
const results = []

for (const topic of topics) {
  try {
    const data = await researchTopic(topic)
    results.push(data)
    console.log(`✅ 完成：${topic}`)
  } catch (error) {
    console.error(`❌ 失败：${topic}`, error.message)
  }

  // 延迟，避免被封 IP
  await new Promise(resolve => setTimeout(resolve, 2000))
}

// 保存到文件
fs.writeFileSync(
  'data/batch-1-rokkoyo.json',
  JSON.stringify(results, null, 2)
)

console.log(`\n📝 已生成 ${results.length} 条数据，保存到 data/batch-1-rokkoyo.json`)
console.log(`\n下一步：访问 http://localhost:3000/admin/bulk-import 导入数据`)
```

---

### 脚本 2：验证已填充内容的质量

```javascript
// scripts/validate-content-quality.js

async function validateEntry(entry) {
  const issues = []

  // 检查字段完整性
  if (!entry.nameZh || entry.nameZh.length < 2) {
    issues.push('中文名称过短')
  }

  if (!entry.description || entry.description.length < 100) {
    issues.push('描述过短（至少 100 字）')
  }

  if (!entry.sources || entry.sources.length < 2) {
    issues.push('来源不足（至少 2 个）')
  }

  // 检查 URL 有效性
  for (const source of entry.sources) {
    try {
      const response = await fetch(source.url, { method: 'HEAD' })
      if (!response.ok) {
        issues.push(`来源链接失效：${source.url}`)
      }
    } catch (error) {
      issues.push(`来源链接无法访问：${source.url}`)
    }
  }

  // 检查日文名称格式
  if (entry.nameJa && !/[\u3040-\u309F\u30A0-\u30FF\u4E00-\u9FAF]/.test(entry.nameJa)) {
    issues.push('日文名称格式可能有误')
  }

  return {
    entryId: entry.id,
    nameZh: entry.nameZh,
    valid: issues.length === 0,
    issues
  }
}

// 批量验证
const entries = await fetchAllEntries()
const report = []

for (const entry of entries) {
  const result = await validateEntry(entry)
  report.push(result)

  if (!result.valid) {
    console.log(`⚠️  ${result.nameZh}`)
    result.issues.forEach(issue => console.log(`   - ${issue}`))
  }
}

// 生成报告
const invalidCount = report.filter(r => !r.valid).length
console.log(`\n📊 验证完成：`)
console.log(`   总条目数：${report.length}`)
console.log(`   有效条目：${report.length - invalidCount}`)
console.log(`   问题条目：${invalidCount}`)

// 保存报告
fs.writeFileSync(
  'data/quality-report.json',
  JSON.stringify(report, null, 2)
)
```

---

## 📊 内容质量标准

### 优秀（A 级）
- ✅ 所有字段完整
- ✅ 描述 > 300 字，结构清晰
- ✅ 来源 ≥ 3 个，全部权威
- ✅ 图片 ≥ 3 张，清晰美观
- ✅ 关键词 ≥ 8 个
- ✅ 无事实错误

### 良好（B 级）
- ✅ 必填字段完整
- ✅ 描述 > 200 字
- ✅ 来源 ≥ 2 个
- ✅ 图片 ≥ 1 张
- ✅ 关键词 ≥ 5 个

### 合格（C 级）
- ✅ 必填字段完整
- ✅ 描述 > 100 字
- ✅ 来源 ≥ 1 个
- ⚠️ 标注"待完善"

### 不合格（需修改）
- ❌ 缺少必填字段
- ❌ 描述 < 100 字
- ❌ 无来源
- ❌ 包含明显错误

---

## 🚨 常见错误与避免

### 错误 1：直接复制粘贴网站内容

**问题**：可能侵权，且表达不自然

**正确做法**：
- 阅读多个来源
- 用自己的语言总结
- 标注参考来源

### 错误 2：信息未验证就填充

**问题**：可能传播错误信息

**正确做法**：
- 至少 2 个来源确认
- 冲突信息查找官方说明
- 不确定的标注"待确认"

### 错误 3：日文名称错误

**问题**：影响专业性

**正确做法**：
- 使用官方日文名称
- 不要使用简化或俗称
- 对比多个权威来源

### 错误 4：来源链接失效

**问题**：无法验证信息

**正确做法**：
- 填充前用 WebFetch 验证链接
- 使用稳定的官方网站
- 避免使用可能变动的页面

### 错误 5：描述过于简短

**问题**：信息量不足

**正确做法**：
- 描述至少 300 字
- 包含历史、特点、意义
- 结构清晰（分段）

---

## 📞 遇到问题时

**无法找到权威来源**：
- 在 DAILY-LOG.md 中记录
- 标注"待确认"
- 等待用户指导

**信息冲突**：
- 列出所有来源和冲突内容
- 在 DAILY-LOG.md 中记录
- 等待用户决策

**技术术语不确定**：
- 保留日文原文
- 添加"（日文：XXX）"说明
- 在 DAILY-LOG.md 中标注

---

**文档版本**: 1.0
**最后更新**: 2026-02-24
